import 'package:flutter/material.dart';

class SleepPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sleep Page'),
      ),
      body: Center(
        child: Text('This is the Sleep Page'),
      ),
    );
  }
}
