import 'package:flutter/material.dart';

class StepsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Steps Page'),
      ),
      body: Center(
        child: Text('This is the Steps Page'),
      ),
    );
  }
}
