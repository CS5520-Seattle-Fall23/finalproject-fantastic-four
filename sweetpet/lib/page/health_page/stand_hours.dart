import 'package:flutter/material.dart';

class StandHoursPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Stand Hours Page'),
      ),
      body: Center(
        child: Text('This is the Stand Hours Page'),
      ),
    );
  }
}
