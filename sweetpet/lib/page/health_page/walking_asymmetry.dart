import 'package:flutter/material.dart';

class WalkingAsymmetryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Walking Asymmetry Page'),
      ),
      body: Center(
        child: Text('This is the Walking Asymmetry Page'),
      ),
    );
  }
}
